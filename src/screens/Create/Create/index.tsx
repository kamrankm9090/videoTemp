import React from 'react';
import {AppContainer, ScreensHeader} from '~/components';

export default function CreateScreen() {
  return (
    <AppContainer>
      <ScreensHeader title="Create" />
    </AppContainer>
  );
}
