import React from 'react';
import {AppContainer, ScreensHeader} from '~/components';

export default function OffersScreen() {
  return (
    <AppContainer>
      <ScreensHeader title="Offers" />
    </AppContainer>
  );
}
